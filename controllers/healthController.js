export const uploadHealthData = (req, res) => {
  res.send("Health data upload route working (stub)");
};

export const getHealthRecords = (req, res) => {
  res.send("Get health records route working (stub)");
};

export const processPrescription = (req, res) => {
  res.send("Prescription OCR route working (stub)");
};
