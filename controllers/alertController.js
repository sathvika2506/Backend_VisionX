export const sendAlert = (req, res) => {
  res.send("General alert route working (stub)");
};

export const getAlerts = (req, res) => {
  res.send("Fetching alerts (stub)");
};

export const sendSMSAlert = (req, res) => {
  res.send("SMS alert route working (stub)");
};

export const sendIVRAlert = (req, res) => {
  res.send("IVR alert route working (stub)");
};
