export const createGroup = (req, res) => {
  res.send("Group creation route working (stub)");
};
export const getGroupInfo = (req, res) => {
  res.send("Get group info route working (stub)");
};
export const traceGroup = (req, res) => {
  res.send("Group tracing route working (stub)");
};
